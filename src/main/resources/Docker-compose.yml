services:
  postgres:
    image: postgres
    container_name: container_postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: ${USER_POSTGRES}
      POSTGRES_USER: ${PASSWORD_POSTGRES}
    ports:
        - "5432:5432"
